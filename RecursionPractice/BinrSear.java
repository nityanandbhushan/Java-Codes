package RecursionPractice;

import chapters.chapter3.chapter_4.chapter__5.chapter_6.chapter_7.binoFactorial;

public class BinrSear {
    public static int printSearch(int arr[],int find,int i){ 
        if(i==0)
    }
     /* public static int binsearch(int arr[],int find,int i){
        /*binsearch(int arr[],int find,int strt,int end) 
        if(i==0){ 
            return -1;
        } 
        else if(arr[i-1]==find){ 
            return i;
 
        } 
        binsearch(arr, find,i-1);



   if(strt==0||end==0){ 
    System.out.println("Not foudn");
     } 
     int mid=(strt+end)/2;
     if(find=arr[mid]){ 
        System.out.println("found");
     } 
     else if(find >arr[mid]){ 
        binsearch(arr, find, mid+1, end);
     }  
     else{ 
        binsearch(arr, find, strt, mid-1);
     }*/
    public static void main(String args[]){ 
        int arr[]={1,2,3,4,5,6,7,8}; 
        int find=5; 
        int strt=0;
        int i=arr.length; 

          /* int bin  =binsearch(arr,find,i); 
        if(bin==-1){ 
            System.out.println("Not foudn"+bin);
        } 
        else{  
            System.out.println("Not foudn"+bin);
            
        }
      binsearch(int arr[],int find,int strt,int end) */
        
    }
}
